import { sortedListToBST } from './convert-sorted-list-to-binary-search-tree';

describe('Convert Sorted List to Binary Search Tree', () => {
  it('evaluates correctly', () => {
    // expect().toEqual();
  });
});

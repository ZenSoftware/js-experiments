import { removeNthFromEnd, ListNode } from './remove-nth-node-from-end-of-list';

describe('Remove Nth Node From End of List', () => {
  it('evaluates correcty', () => {
    // expect(removeNthFromEnd()).toEqual();
  });
});

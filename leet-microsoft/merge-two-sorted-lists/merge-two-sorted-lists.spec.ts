import { mergeTwoLists } from './merge-two-sorted-lists';

describe('Median of Two Sorted ArraysMerge Two Sorted Lists', () => {
  it('evaluates median correcty', () => {
    // expect().toEqual();
  });
});
